import 'package:flutter/material.dart';
class SettingsScreen extends StatefulWidget { const SettingsScreen({super.key}); @override State<SettingsScreen> createState()=>_SettingsState(); }
class _SettingsState extends State<SettingsScreen>{bool notifications=true,offline=false,dark=false;@override Widget build(BuildContext context){return Scaffold(appBar:AppBar(title:const Text('الإعدادات')),body:ListView(padding:const EdgeInsets.all(20),children:[const Text('التفضيلات',style:TextStyle(fontSize:20,fontWeight:FontWeight.bold)),SwitchListTile(title:const Text('إشعارات القراءة'),value:notifications,onChanged:(v)=>setState(()=>notifications=v)),SwitchListTile(title:const Text('الوضع غير المتصل'),value:offline,onChanged:(v)=>setState(()=>offline=v)),SwitchListTile(title:const Text('المظهر الداكن'),value:dark,onChanged:(v)=>setState(()=>dark=v)),const Divider(),const ListTile(title:Text('اللغة'),trailing:Text('العربية')),const ListTile(title:Text('مزامنة التقدم'),trailing:Icon(Icons.chevron_left)),const ListTile(title:Text('عن BookSoul'),subtitle:Text('الإصدار 1.0.0'))]));}}
