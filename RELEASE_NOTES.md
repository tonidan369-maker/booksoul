# BookSoul v1.4.0 — ملاحظات الإصدار

## ملخص الإصدار

يوحّد هذا الإصدار تطبيق القراءة الرئيسي ولوحة الإدارة ذات التصميم المحدّث، ويضيف استضافة محلية للوحة الإدارة عبر Docker Compose وخدمة تحكم مستقلة. يركّز الإصدار على تجربة قراءة عربية متجاوبة، وإدارة مركزية قابلة للتوسع، وفصل الأسرار والتكاملات الحساسة عن تطبيقات Flutter.

| الحزمة | النسخة | الحالة |
|---|---:|---|
| BookSoul Main | `1.4.0+4` | APK إصدار فعلي، ومصدر Windows جاهز للبناء على Windows |
| BookSoul Dashboard | `1.4.0+4` | Flutter Web + Docker/Dokploy + استضافة محلية |
| BookSoul Control Plane | `1.0.0` | Node.js/Docker، تحقق صحي ومسارات تحكم محمية |

## التطبيق الرئيسي

تمت إعادة تصميم المكتبة بهوية BookSoul الدافئة: رأس تحريري، بحث واضح، وسوم موضوعية، بطاقات متجاوبة من عمودين حتى خمسة أعمدة، حالة مفضلة، وتقدم قراءة مرئي. يستمر القارئ في حفظ التقدم والعلامات والملاحظات وتفضيلات القراءة محليًا، مع دعم المشاركة والـ RTL.

## لوحة الإدارة

تضم لوحة الإدارة الآن إحصاءات التطبيق والتنزيلات من جدول `app_metrics_daily`، وحالة الإصدار والاستقرار وزمن API، إضافة إلى استوديو تكاملات لاختبار مسارات API ومراقبة حالة MCP ومزودي النماذج ووكيل التحكم. لا تُنشئ الواجهة أرقامًا تجريبية؛ تظهر حالة واضحة إلى أن تصل بيانات القياس الفعلية إلى Supabase.

## الاستضافة المحلية وDokploy

أضيفت خدمة `control-plane` خلف Dashboard. توفر `/healthz` ومسارات محمية بتوكن للتحقق من الحالة والتكاملات واختبارات API ضمن قائمة سماح ومحادثة وكيل في وضع خطة غير منفذة. أضيفت ملفات `docker-compose.local.yml` و`Dockerfile` و`.env.example` و`LOCAL_HOSTING.md`. لا تُكشف خدمة التحكم للعامة في تكوين Dokploy.

## الأمان

لا تضم Flutter Web أي Service Role Key أو مفتاح مزود نماذج أو مفتاح MCP. تحفظ الأسرار داخل متغيرات Docker/Dokploy فقط. لا يسمح مختبر API بعناوين URL حرة، ويظل الوكيل في وضع التخطيط إلى أن تُضاف سياسة أدوات وصلاحيات وتدقيق أحداث.

## المواد التسويقية

أضيف مسار `marketing/store-screenshots/` لتبني محرر `ParthJadhav/app-store-screenshots` كعملية مستقلة لإنشاء لقطات Google Play وApp Store قابلة للتكرار. لا يدخل محرر Next.js ضمن runtime تطبيق Flutter.

## التحقق

تم اجتياز تحليل واختبارات Flutter للتطبيق ولوحة الإدارة، وبناء Flutter Web للوحة، وبناء APK إصدار فعلي للتطبيق الرئيسي. تم تشغيل خدمة التحكم محليًا والتحقق من `/healthz` و`/api/status` الموثق بتوكن تجريبي. لم يُبنَ EXE محليًا لأن Flutter Windows يتطلب مضيف Windows؛ يوجد مصدر Windows وسكربت البناء داخل حزمة التطبيق.
